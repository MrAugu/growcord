const Discord = require("discord.js"); // eslint-disable-line no-unused-vars
const fs = require("fs");

module.exports = class {
  constructor (client) {
    this.client = client;
  }

  async run (wotdName) {
    /* To Do */
  }
};